#include "main.h"
#include <stdio.h>
/**
 * _abs - value absolute
 * 
 * @n: The int to print
 * Return: Always ).
 */
int print_last_digit(int n)
{
	int r;
	r = print_last_digit(98);
	{
		_putchar('0' + r);
		_putchar('0' + (n / 10));
	}
	return (0);
}
