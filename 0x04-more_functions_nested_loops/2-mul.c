#include "main.h"
#include <stdio.h>

/**
 * mul -multiplies two intergers.
 * @a: character a.
 * @b: character b.
 * Return: multiplication.
 */
int main(void)
{
	printf("%d\n", mul(a * b));
	return (0);
}
