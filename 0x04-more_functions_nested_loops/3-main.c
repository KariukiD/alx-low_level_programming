#include "main.h"
/**
 * main - print_numbers
 * print_numbers - prints numbers
 * Return: Always 0.
 */
int main(void)

{
	int (i);

	for (i = 0; i < 10; i++)
	{
	    print_numbers();
	}
	return (0);
}
