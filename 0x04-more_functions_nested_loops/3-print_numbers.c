#include "main.h"
#include <stdio.h>
/**
 * print_most_numbers - print 0 to 9
 *
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	int num;

	for (num = 0; num < 10; num++)
		putchar(num);
	putchar('\num');
	return (0);
}
